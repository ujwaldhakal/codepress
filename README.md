# Docker setup for php / mysql / nginx / phpmyadmin

Usage
 
* All nginx config are inside `docker/default.conf`
* All php config are inside `php.ini`
* For virtual host default config look at file `docker-compose.yml`

